# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven
env:
  sys.registry.pwd: ${{ secrets.REGISTRY_PWD }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
on:
  issues:
    types: [opened]



jobs:
  build:

    runs-on: ubuntu-latest

    # 确保权限，以便工作流可以修改评论
    permissions:
      issues: write

    #if: contains(github.event.comment.body, 'pull')

    steps:
    - name: Reply and Mark as Running (Optional)
      run: gh issue comment ${{ github.event.issue.number }} --body "🤖 任务已收到，正在执行中..." --repo ${{ github.repository }}

    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn  package -q  && mv target/app.jar ./


    - name: Run jar
      id: jar
      run: java -jar app.jar ${{ github.event.issue.title }} server

      # --- 步骤 3: 修改原始评论 (核心步骤) ---
    - name: Edit Original Comment with Status
      # 使用 GitHub CLI 来编辑触发工作流的原始评论
      run: gh issue comment ${{ github.event.issue.number }} --body "✅ 任务已完成, 目标镜像：${{ steps.jar.outputs.image }}" --repo ${{ github.repository }}




