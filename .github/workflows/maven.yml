# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven
env:
  sys.registry.pwd: ${{ secrets.REGISTRY_PWD }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
on:
  issue_comment:
    # 仅在创建新的评论时触发工作流
    types: [created,edited]
    # 您也可以添加 edited 和 deleted



jobs:
  build:

    runs-on: ubuntu-latest

    # 确保权限，以便工作流可以修改评论
    permissions:
      issues: write

    #if: contains(github.event.comment.body, 'pull')

    steps:
    - name: Reply and Mark as Running (Optional)
      run: |
        gh issue comment ${{ github.event.issue.number }} \
          --body "🤖 任务已收到，正在执行中... (Comment ID: ${{ github.event.comment.id }})" \
          --repo ${{ github.repository }}

    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn  package -q  && mv target/app.jar ./


    - name: Run jar
      run: java -jar app.jar ${{ github.event.comment.body }} server

      # --- 步骤 3: 修改原始评论 (核心步骤) ---
    - name: Edit Original Comment with Status
      # 使用 GitHub CLI 来编辑触发工作流的原始评论
      run: |
        NEW_BODY="## ✅ 任务已完成 (Updated by Actions)
        
        您在 ${{ github.event.repository.full_name }} 的 Issue/PR #${{ github.event.issue.number }} 上发出的命令已被执行。
        
        **执行结果:**
        ${{ steps.main_task.outputs.result_message }}
        "
        
        gh api \
          --method PATCH \
          -H "Accept: application/vnd.github+json" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          /repos/${{ github.repository }}/issues/comments/${{ github.event.comment.id }} \
          -f body="$NEW_BODY"


